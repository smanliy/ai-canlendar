/** Resolves ACP integer options through the shared normalization contract. */
export declare function resolveIntegerOption(value: number | undefined, fallback: number, params: {
    min: number;
}): number;
